name: CI

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: lts/*

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json

      - name: Install frontend dependencies
        run: bun install --frozen-lockfile

      - name: Run fast repository checks
        run: |
          bun run version:check
          bun run repo:check-binaries
          bun run typecheck
          bun run lint
          bun run i18n:audit

      - name: Run frontend tests
        run: bun run test

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev

      - name: Build AI runtime sidecar
        run: bun run build:ai-runtime

      - name: Run Rust tests
        env:
          GIT_AUTHOR_NAME: Macro CI
          GIT_AUTHOR_EMAIL: ci@macro.invalid
          GIT_COMMITTER_NAME: Macro CI
          GIT_COMMITTER_EMAIL: ci@macro.invalid
        run: cargo test --manifest-path src-tauri/Cargo.toml --locked -- --test-threads=1

      - name: Check headless example
        run: cargo check --manifest-path src-tauri/Cargo.toml --example macro-headless --locked

      - name: Build frontend
        run: bun run build

      - name: Check bundle budgets
        run: bun run bundle:check
